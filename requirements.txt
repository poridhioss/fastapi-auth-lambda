# Lab 4 dependencies. Bundled into the Lambda zip alongside the app code.
fastapi>=0.115.0
mangum>=0.18.0
pydantic>=2.7.0
pyjwt>=2.8.0
bcrypt>=4.1.0
boto3>=1.34.0
botocore>=1.34.0
uvicorn[standard]>=0.30.0   # only for `uvicorn app.main:app` local testing